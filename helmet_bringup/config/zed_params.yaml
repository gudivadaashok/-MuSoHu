# ZED Camera Parameters
zed_node:
  ros__parameters:
    # Camera configuration
    camera_model: "zed2"
    camera_name: "zed2"
    
    # Resolution and FPS
    resolution: 2  # 0=HD2K, 1=HD1080, 2=HD720, 3=VGA
    fps: 30.0
    
    # Depth configuration
    depth_mode: 1  # 0=NONE, 1=PERFORMANCE, 2=QUALITY, 3=ULTRA
    depth_stabilization: true
    depth_maximum_distance: 20.0  # meters
    depth_minimum_distance: 0.3   # meters
    
    # Coordinate frames
    camera_frame: "zed2_camera_center"
    left_camera_frame: "zed2_left_camera_frame"
    right_camera_frame: "zed2_right_camera_frame"
    depth_frame: "zed2_depth_camera_frame"
    
    # Publishing configuration
    publish_rgb: true
    publish_depth: true
    publish_stereo: true
    publish_cloud: false
    publish_map_tf: false
    
    # Topic remapping
    rgb_topic: "rgb/image_rect_color"
    depth_topic: "depth/depth_registered"